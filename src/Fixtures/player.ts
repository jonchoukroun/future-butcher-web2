export type PlayerStatsType = {
    name: string;
    cash: number;
    debt: number;
    health: number;
};

export const player: PlayerStatsType = {
    name: "Bob",
    cash: 5000,
    debt: 5000,
    health: 100,
};
